name: e2e
on:
  push:
    branches: [main]
  pull_request:

jobs:
  compose-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - run: bash infra/e2e-smoke.sh
