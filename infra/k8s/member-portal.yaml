apiVersion: apps/v1
kind: Deployment
metadata:
  name: member-portal
  namespace: osprey
  labels:
    app: member-portal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: member-portal
  template:
    metadata:
      labels:
        app: member-portal
    spec:
      containers:
        - name: member-portal
          image: osprey-loyalty-member-portal:latest
          imagePullPolicy: Never # kind: image is side-loaded
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: member-portal
  namespace: osprey
  labels:
    app: member-portal
spec:
  type: ClusterIP
  selector:
    app: member-portal
  ports:
    - port: 80
      targetPort: 80
