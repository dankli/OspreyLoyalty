apiVersion: apps/v1
kind: Deployment
metadata:
  name: route-explorer
  namespace: osprey
  labels:
    app: route-explorer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: route-explorer
  template:
    metadata:
      labels:
        app: route-explorer
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: route-explorer
          image: osprey-loyalty-route-explorer:latest
          imagePullPolicy: Never # kind: image is side-loaded
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 101 # nginx user in the unprivileged image
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: tmp # nginx-unprivileged writes pid + *_temp under /tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: route-explorer
  namespace: osprey
  labels:
    app: route-explorer
spec:
  type: ClusterIP
  selector:
    app: route-explorer
  ports:
    - port: 80
      targetPort: 8080 # unprivileged nginx; Service keeps 80 so ingress is unchanged
