name: partners
on:
  push:
    branches: [main]
    paths: ["services/partners/**", ".github/workflows/partners.yml"]
  pull_request:
    paths: ["services/partners/**", ".github/workflows/partners.yml"]

jobs:
  build-test:
    runs-on: ubuntu-latest
    defaults:
      run: { working-directory: services/partners }
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
      - run: ./mvnw -q test
