name: gateway
on:
  push:
    branches: [main]
    paths: ["services/gateway/**", ".github/workflows/gateway.yml"]
  pull_request:
    paths: ["services/gateway/**", ".github/workflows/gateway.yml"]

jobs:
  build-test:
    runs-on: ubuntu-latest
    defaults:
      run: { working-directory: services/gateway }
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build
